import { DefaultSeoProps } from 'next-seo'

const config: DefaultSeoProps = {
  title: 'Apple TH Inventory Checker',
  description: 'Apple TH Inventory Checker',
  titleTemplate: '%s | Apple TH Inventory Checker',
}

export default config
